# Syndigo Attribute Count Checker

This Streamlit app lets any Syndigo customer check attribute record counts for any entity by connecting to the Syndigo API.

## 🚀 Features
- Enter tenant, entity, and API credentials interactively
- Upload attribute list (CSV)
- Retrieves record counts via Syndigo API
- Download results as CSV or Excel
- Clean Syndigo-themed UI with logo and color scheme

## 🧩 Setup

1. Clone the repository
2. Install dependencies:
   ```bash
   pip install -r requirements.txt
